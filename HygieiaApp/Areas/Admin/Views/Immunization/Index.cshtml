@model System.Collections.Generic.IEnumerable<HygieiaApp.Models.Models.Immunization>

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Medications</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Immunization" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle-dotted"></i> &nbsp; Add new immunization
            </a>
        </div>
        
        <br/>
        <br/>
        <br/>
        <br/>
      <table class="table table table-bordered border-1.4 border-blue">
                  <thead class="thead-dark text-secondary" style="background-color: lightblue; text-overline-color: white; color: white">
                  <tr class="text-primary">
                      <th>
                          Type
                      </th>
                      <th>
                          Used For
                      </th>

                      <th>
                          
                      </th>
                     
                  </tr>
                  </thead>
                  <tbody>
                  @foreach (var mc in Model)
                  {
                      <tr>
                          <td>@mc.Type</td>
                          <td>@mc.UsedFor</td>
                          <td>
                              <div class="w-75 btn-group" role="group">
                                  <a asp-controller="Immunization" asp-action="Edit" asp-route-id="@mc.Id" class="btn btn-primary mx-2">
                                      <i class="bi bi-pencil-square"></i> Edit
                                  </a> &nbsp;
                                  <a asp-controller="Immunization" asp-action="Delete" asp-route-id="@mc.Id" class="btn btn-primary mx-2">
                                      <i class="bi bi-trash"></i> Delete
                                  </a>
                              </div>
                          </td>
                      </tr>
                  }
                  </tbody>
              </table>
    </div>
</div>